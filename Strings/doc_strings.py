def greet(name):
    """
    Greets the user by name.

    Parameters:
    name (str): The name of the user to greet.

    Returns:
    str: A greeting message.
    """
    return f"Hello, {name}!"

print(greet.__doc__)